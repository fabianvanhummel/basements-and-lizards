import React from "react";
import { OverviewTab } from "./OverviewTab";

export default {
  title: "BM App/OverviewTab",
  component: OverviewTab,
};

const Template = (args) => <OverviewTab />;

export const Primary = Template.bind({});
Primary.args = {};
